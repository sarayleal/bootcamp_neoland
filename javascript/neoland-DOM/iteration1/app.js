/*  1.1 Usa querySelector para mostrar por consola el botón con la clase .**showme** */
const showmeButton = document.querySelector('.showme')
console.log(showmeButton);

/*  1.2 Usa querySelector para mostrar por consola el h1 con el id #pillado */
const h1Id = document.querySelector('#pillado')
console.log(h1Id)

/* 1.3 Usa querySelector para mostrar por consola todos los p */
const h1PNode = document.querySelectorAll('p')
console.log(h1PNode)


/* 1.4 Usa querySelector para mostrar por consola todos los elementos con la clase.pokemon */
const pokemonClass = document.querySelectorAll('.pokemon')
console.log(pokemonClass)

/* 1.5 Usa querySelector para mostrar por consola todos los elementos con el atributo 
data-function="testMe" */
const testMe = document.querySelectorAll('Span')
console.log(testMe)

/* 1.6 Usa querySelector para mostrar por consola el 3 personaje con el atributo 
data-function="testMe".  */
const characterRick = document.querySelectorAll('[data-function="testMe"]')
console.log(characterRick)















